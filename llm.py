from __future__ import annotations

import json
import logging

from pathlib import Path
from datetime import datetime

logger = logging.getLogger("app.llm")


def save_llm_interaction(
    system_prompt: str,
    user_prompt: str,
    response: str,
) -> None:
    """
    Сохраняет полный запрос и ответ модели.
    """

    PROJECT_ROOT = Path(__file__).resolve().parent
    log_dir = PROJECT_ROOT / "outputs" / "judge_logs"
    log_dir.mkdir(parents=True, exist_ok=True)

    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S_%f")

    file_path = log_dir / f"judge_{timestamp}.txt"

    with open(file_path, "w", encoding="utf-8") as f:

        f.write("=" * 80)
        f.write("\nSYSTEM PROMPT\n")
        f.write("=" * 80)
        f.write("\n\n")
        f.write(system_prompt)

        f.write("\n\n")
        f.write("=" * 80)
        f.write("\nUSER PROMPT\n")
        f.write("=" * 80)
        f.write("\n\n")
        f.write(user_prompt)

        f.write("\n\n")
        f.write("=" * 80)
        f.write("\nMODEL RESPONSE\n")
        f.write("=" * 80)
        f.write("\n\n")
        f.write(response)

    logger.info("LLM interaction saved to %s", file_path)


JUDGE_SYSTEM_PROMPT = """
Ты выступаешь в роли независимого медицинского эксперта.

Тебе будут переданы:

1. Заметка врача (Ground Truth).
2. Рекомендации, сгенерированные системой.

Твоя задача — оценить качество рекомендаций системы.

ВАЖНО

Заметка врача написана на английском языке, рекомендации системы —
на русском языке. Сравнивай их ПО КЛИНИЧЕСКОМУ СМЫСЛУ
(диагнозы, препараты, состояния, факторы риска),
а не по совпадению языка, терминов или формулировок.
Один и тот же препарат или диагноз может быть назван по-разному
на двух языках — это не является основанием снижать оценку.

ВАЖНО

Система предназначена для формирования рекомендаций
по заболеваниям пациента.

При оценке используй ТОЛЬКО:

- диагнозы;
- хронические заболевания;
- факторы риска;
- анамнез;
- назначенную медикаментозную терапию;
- клинически значимую информацию.

НЕ учитывай:

- организационные инструкции;
- информацию о госпитализации;
- историю операции;
- уход за послеоперационной раной;
- рекомендации по уходу за швами;
- ограничения после операции;
- рекомендации по вождению автомобиля;
- рекомендации по подъему тяжестей;
- бытовые рекомендации;
- контактную информацию.

Отсутствие подобных рекомендаций НЕ должно снижать оценки
coverage, precision, safety или usefulness.

========================================

Оцени следующие критерии по шкале от 0 до 10.

coverage

Насколько рекомендации покрывают выявленные заболевания,
хронические состояния и клинически значимые факторы риска,
описанные в заметке врача.

Не учитывай послеоперационные инструкции
и бытовые рекомендации.

----------------------------------------

precision

Насколько каждая рекомендация подтверждается
информацией из заметки врача.

Рекомендация считается подтвержденной,
если она имеет явное основание
в диагнозах,
анамнезе,
лекарственной терапии
или других медицинских данных пациента.

----------------------------------------

safety

Есть ли потенциально опасные,
противоречивые
или вредные медицинские рекомендации.

Если рекомендаций опасного характера нет,
ставь высокую оценку.

Отсутствие некоторых рекомендаций
НЕ снижает safety.

----------------------------------------

usefulness

Насколько рекомендации практически полезны
для данного пациента
с учетом его заболеваний
и клинической картины.

========================================

Также необходимо определить.

missed_information

Какие важные заболевания,
состояния,
факторы риска
или медикаментозная терапия
из заметки врача
не были отражены рекомендациями.

Не включай сюда:

- уход за раной;
- послеоперационные ограничения;
- организационные инструкции;
- бытовые рекомендации.

----------------------------------------

unsupported_recommendations

Какие рекомендации присутствуют в ответе системы,
но НЕ имеют достаточного основания
в заметке врача.

----------------------------------------

dangerous_recommendations

Какие рекомендации потенциально опасны
или могут нанести вред пациенту.

Если таких нет —
верни пустой список.

========================================

Для каждой оценки обязательно напиши краткое объяснение.

Верни СТРОГО JSON следующего формата.

{
    "coverage": 0,
    "precision": 0,
    "safety": 0,
    "usefulness": 0,

    "missed_information": [],

    "unsupported_recommendations": [],

    "dangerous_recommendations": [],

    "reasoning": {
        "coverage": "",
        "precision": "",
        "safety": "",
        "usefulness": ""
    }
}

Требования:

- Ответ должен содержать ТОЛЬКО JSON.
- Не используй Markdown.
- Не используй ```json.
- Не добавляй пояснений.
- Не добавляй новых полей.
- Все оценки — целые числа от 0 до 10.
- Все списки должны содержать строки.
- Если список пуст — верни [].
- JSON должен быть корректным.
"""


def loads_lenient(content: str):

    text = content.strip()

    if text.startswith("```"):

        text = text.strip("`")

        if text.lstrip().lower().startswith("json"):

            text = text.lstrip()[4:]

    return json.loads(text)